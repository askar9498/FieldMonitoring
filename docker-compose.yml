version: "3.8"

services:
  web:
    container_name: web
    platform: linux/amd64
    build:
      context: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"